# -*- mode: ruby -*-
# # vi: set ft=ruby :
#
source ENV['GEM_SOURCE'] || 'https://rubygems.org'

group :development, :test do
  gem 'rake'

  gem 'serverspec'
  gem 'docker-api', :require => 'docker'
end